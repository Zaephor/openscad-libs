# sbc (library)

TODO: what real-world data this library provides and the units used.

## Import

```scad
use <sbc/sbc.scad>;
```

## Sources

List the standards / references the values come from (also in `lib.json`).
