// hardware library — concrete fastener/standard dimensions (mm).
// Imported as: use <hardware/hardware.scad>;

// ISO metric clearance hole diameters (close fit), mm.
function m3_clearance_mm() = 3.4;
function m4_clearance_mm() = 4.5;
function m5_clearance_mm() = 5.5;
